import "../stdlib/integer.🍆"
import "../stdlib/list.🍆"

struct Main {
	constructor(){};
	fun void main(){
		auto out = new StdOut();
		var List<Integer> ints = new List<Integer>();
		for(var int i = 0; i < 10; i = i + 1) {
			ints.push_back(new Integer(i));
		}
		out.print_int_ln(ints.size());
		ints.print(:out);
		out.print_newline();
	};
}
